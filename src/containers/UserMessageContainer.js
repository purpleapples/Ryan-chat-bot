import UserMessageComponent from '../components/UserMessageComponent';

const UserMessageContainer = ({text}) => {
    return (
        <UserMessageComponent text={text}/>                    
    );
};

export default UserMessageContainer;